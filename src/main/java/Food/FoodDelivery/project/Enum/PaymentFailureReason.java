package Food.FoodDelivery.project.Enum;

public enum PaymentFailureReason {
    INSUFFICIENT_FUNDS,
    NETWORK_ERROR,
    PAYMENT_TIMEOUT,
    INVALID_CARD,
    CANCELLED_BY_USER,
    INVALID_UPI,
    FRAUD_DETECTED,
    UNKNOWN_ERROR,
    INVALID_PAYMENT_SIGNATURE
}
